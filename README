Source code for mini-SQL language built for compiler structure final

BNF Notation for the project
<query>      ::= "SELECT" <field_list> "FROM" <identifier> [ "WHERE" <bool_expr> ]
<field_list> ::= "*" | <identifier> { "," <identifier> }
<bool_expr>  ::= <bool_term> { "OR" <bool_term> }
<bool_term>  ::= <bool_factor> { "AND" <bool_factor> }
<bool_factor>::= "(" <bool_expr> ")" | <predicate>
<predicate>  ::= <identifier> <comp_op> <literal>
<comp_op>    ::= "=" | "!=" | "<" | "<=" | ">" | ">="
<literal>    ::= <number> | <string> | "true" | "false"
<identifier> ::= letter { letter | digit | "_" }
<number>     ::= digit { digit } [ "." digit { digit } ]
<string>     ::= '"' { any_character_except_quote } '"'
